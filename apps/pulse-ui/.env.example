# Pulse API (NestJS) — REST base URL including /v1 prefix
VITE_API_BASE_URL=http://localhost:3000/v1

# Pulse Analytics (FastAPI) — base URL for SSE streaming
VITE_ANALYTICS_BASE_URL=http://127.0.0.1:8000
